version: '3.2'

services:
  gtest:
    build:
      context: .
      dockerfile: Dockerfile
    image: john04047210/ubuntu-gtest
    container_name: 'gtest-ubuntu'
    restart: 'no'
    command: /sbin/init
    privileged: true
    hostname: gtest
    domainname: neusoft.co.jp
